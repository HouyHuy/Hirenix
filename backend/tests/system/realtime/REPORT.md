# Realtime Test Report

Run with:

```powershell
powershell -ExecutionPolicy Bypass -File ..\..\scripts\run-system-tests.ps1 -Mode Realtime
```

Covers SignalR latency, concurrent message delivery, and a configurable soak mode.
