# Failover and Recovery Test Report

Run destructive API recovery probe with:

```powershell
powershell -ExecutionPolicy Bypass -File ..\..\scripts\run-system-tests.ps1 -Mode Failover -EnableDestructive
```
