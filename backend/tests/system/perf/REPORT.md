# Performance Test Report

Run with:

```powershell
powershell -ExecutionPolicy Bypass -File ..\..\scripts\run-system-tests.ps1 -Mode Perf
```

Metrics captured: p50/p95/p99 latency, throughput, and error rate for core backend endpoints.
