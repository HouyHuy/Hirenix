# Security Test Report

Run lightweight JWT/security checks with:

```powershell
powershell -ExecutionPolicy Bypass -File ..\..\scripts\run-system-tests.ps1 -Mode Security
```

Run OWASP ZAP with `-EnableExternal`.
